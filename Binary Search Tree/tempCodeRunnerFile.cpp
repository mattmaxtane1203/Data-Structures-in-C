// Delete student by name
// Student *deletes(Student *root, int _age){

//     // If age is smaller, move to the left
//     if(_age < root->age){
//         root->left = deletes(root->left, _age);
//         return;
//     }

//     // If age is larger, move to the right
//     if(_age > root->age){
//         root->right = deletes(root->right, _age);
//         return;
//     }

//     return root;
// }